// Enum for user roles (USER/ADMIN).
// Used for role-based access control in the application.

package com.theinsightcollective.the_insight_collective_project.models;

public enum Role {
    USER, ADMIN;
}
